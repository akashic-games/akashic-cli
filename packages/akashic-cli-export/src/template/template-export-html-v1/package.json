{
  "name": "@akashic/akashic-cli-export-html-template-v1",
  "version": "0.0.1",
  "description": "Modules for export html template.",
  "scripts": {
    "build": "npm run build:mkdir && npm run copy",
    "build:mkdir": "shx mkdir -p ../../../lib/template/v1/js",
    "copy": "shx cp -r js ../../../lib/template/v1 && shx cp -r css ../../../lib/template/v1"
  },
  "devDependencies": {
    "shx": "0.4.0"
  },
  "private": true
}
